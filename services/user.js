import { PrismaClient } from '@prisma/client'

module.exports = {
    findAll: async (filters, options) => {

        return [];
    },

    findOne: async (id) => {
        return {};
    },

    create: async (data) => {
        return {};
    },
}